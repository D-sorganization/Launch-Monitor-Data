name: Validate Data

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  quality-gate:
    name: quality-gate
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e . pytest ruff
      - run: ruff check .
      - run: python -m pytest
      - run: launch-monitor-data validate
      - run: launch-monitor-data build --output rebuilt
      - name: Verify committed CSV artifacts
        run: diff -ru --exclude='*.sqlite' database rebuilt

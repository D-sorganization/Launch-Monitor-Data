repos:
  - repo: local
    hooks:
      - id: deferred-validation
        name: deferred validation catalog
        entry: python shared_scripts/deferred_validation.py --repo-root .
        language: python
        pass_filenames: false
        always_run: true
